/// Dummy main file. Since this is a library we don't provide a main()
/// usually, but we need one for the unittests. `DFLAGS="-main"` leads
/// to weird linking errors, so just do this for now.
void main() {}
